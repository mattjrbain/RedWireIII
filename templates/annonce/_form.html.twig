{{ form_start(form) }}
{#    {{ form_widget(form) }}#}
    {{ form_row(form.rubrique) }}
    {{ form_row(form.entete, {'label': 'Titre'}) }}
    {{ form_row(form.corps, {'label': 'Contenu'}) }}
    {{ form_row(form.images, {'label': 'Vos images','attr': {'placeholder': 'Télécharger illustration...'}}) }}

<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
