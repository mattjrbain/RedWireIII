{% form_theme form 'annonce/_form_theme.html.twig' %}
{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_row(form.entete, {'label': 'Titre'}) }}
{{ form_row(form.corps) }}
{{ form_row(form.rubrique) }}
{{ form_row(form.images) }}
{#    {{ form_widget(form) }}#}
<button class="btn btn-info btn-sm">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
{# https://bootsnipp.com/snippets/eNbOa A voir pour preview des images #}